package com.sanisamoj.data.models.enums

enum class DetailsMessages(val message: String) {
    SaveAnImageWithSize("Save an image with a size of up to"),
    AllowedImageTypes("The allowed types for images are"),
    AllowedSizeForTotalImages("The allowed total size for images is")
}